export default class Login {
  constructor (senha, senhaValidacao, nomeLogin, ativo) {
    this.senha = senha
    this.senhaValidacao = senhaValidacao
    this.nomeLogin = nomeLogin
    this.ativo = ativo
  }
}
